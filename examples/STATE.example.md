# STATE — Импорт выписок (учебный пример)
Итераций: 2 · Закрыто задач: 1 из 12 · Текущее узкое место: ⛔ сопоставление платежа с договором

> Синтетический пример журнала к `PLAN.example.md`: показать заполненный срез
> и дать `state-lint` материал для прогона. Форма проверяется командой
> `node scripts/state-lint.mjs examples/STATE.example.md`.

## Статус для владельца (обновляется каждую итерацию)

| JTBD-сценарий | Моя выгода (outcome) | Статус | Что сделано (без жаргона) | Доказательство | Следующий шаг |
|---|---|---|---|---|---|
| T-1.1.1 — Разбор выписки в единый вид строк | Загружаю файл банка и вижу строки в таблице, не перенося их руками | ✅ | Импорт разбирает файл и показывает строки | Выписка прошлого месяца загружена целиком, строки в таблице | перейти к отказу на неизвестном формате |
| T-1.1.2 — Отклонять файл неизвестного формата | Понимаю, почему файл не принят, вместо пустого экрана | ⚪ | — | — | написать падающую проверку на чужой формат |

---

## Итерация 1 — 2026-03-04 — T-1.1.1 — Разбор выписки в единый вид строк
- Announce: затрону слой импорта и таблицу строк; зелёной станет `make test SUITE=import`; на экране появятся строки реальной выписки прошлого месяца
- Красная фаза: `make test SUITE=import` → `FAIL import/parse: no parser registered (exit 1)`
- Сделано: парсер двух форматов банка и приведение строк к единому виду; запись строк с признаком источника
- Проверка: `make test SUITE=import` → `ok 14 / 14, exit 0`
- Runtime evidence: загрузил выписку за прошлый месяц через экран импорта — строки видны в таблице, ни одна не потерялась при сверке количества со строками файла
- Вердикт verifier: ✅ — DoD подтверждён выводом команды, runtime evidence относится к этой задаче, «Чтобы» достигнут; ослабленных проверок нет
- Реализующие элементы: слой импорта (парсер, нормализация), экран списка строк
- Ссылка на изменение: перечень файлов приложен к срезу; коммит `a1b2c3d`
- Состояние: CLOSE
- Следующее: T-1.1.2

### Diff vs предыдущий срез
- **Added**: —
- **Changed**: T-1.1.1 ⚪ → ✅
- **Closed**: T-1.1.1 — Разбор выписки в единый вид строк
- **Follow-ups added**: F1 — формат третьего банка встречается в архиве, парсера нет
- **Follow-ups closed**: —

---

## DECISIONS
| # | Решение | Альтернативы, которые отбросил | Почему | Обратимо? |
|---|---|---|---|---|
| D1 | Признак источника хранится у строки, а не у файла | хранить только у файла загрузки | строка живёт дольше файла и попадает в спорные списком | да |

## BLOCKERS
| # | Что заблокировано | Чем именно | Что разблокирует (evidence) | Владелец | Перепроверить когда |
|---|---|---|---|---|---|
| B1 | Замер доли разнесённых строк T-2.1.2 | нет выписки с эталонной разметкой | файл с проставленными вручную договорами | владелец | перед стартом B-2 |

## FOLLOW-UPS
| # | Описание | Связь с задачей | Severity | Recovery action |
|---|---|---|---|---|
| F1 | Формат третьего банка встречается в архиве, парсера нет | T-1.1.2 | 🟡 | отдельная задача после закрытия M-1.1 |

---

*Учебный пример из [Autonomic-Work-Kit](https://github.com/dmitryprg-ai/Autonomic-Work-Kit). © 2026 Dmitry Batulin. Лицензия [CC BY-NC 4.0](https://creativecommons.org/licenses/by-nc/4.0/deed.ru): некоммерческое использование при указании авторства.*
